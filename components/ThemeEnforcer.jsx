'use client'; import { useEffect } from 'react'; /** * ThemeEnforcer component * This component ensures that the website adheres to the requested theme rules: * - On light theme: Background must be white, text must be black. * - It "compels" these styles by applying them to the document body and root. */ export default function ThemeEnforcer() { useEffect(() => { const enforceTheme = () => { const isDark = document.documentElement.classList.contains('dark'); if (!isDark) { document.body.style.backgroundColor = '#ffffff'; document.body.style.color = '#000000'; const sections = document.querySelectorAll('section'); sections.forEach(section => { if (!section.classList.contains('dark:bg-slate-800') && !section.classList.contains('dark:bg-neutral-900')) { } }); } else { document.body.style.backgroundColor = ''; document.body.style.color = ''; } }; enforceTheme(); const observer = new MutationObserver((mutations) => { mutations.forEach((mutation) => { if (mutation.attributeName === 'class') { enforceTheme(); } }); }); observer.observe(document.documentElement, { attributes: true }); return () => observer.disconnect(); }, []); return null; // This component doesn't render anything }
