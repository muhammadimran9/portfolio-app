'use client'; export default function ThemedCard({ children, className = '', hover = false, padding = 'md', ...props }) { const baseClasses = 'theme-card rounded-lg transition-all duration-300'; const paddings = { none: '', sm: 'p-3', md: 'p-4', lg: 'p-6', xl: 'p-8' }; const hoverClasses = hover ? 'hover:theme-shadow hover:scale-105 cursor-pointer' : ''; const paddingClasses = paddings[padding] || paddings.md; return ( <div className={`${baseClasses} ${hoverClasses} ${paddingClasses} ${className}`} style={{ background: 'var(--card-bg)', border: '1px solid var(--card-border)', color: 'var(--foreground)' }} {...props} > {children} </div> ); }
